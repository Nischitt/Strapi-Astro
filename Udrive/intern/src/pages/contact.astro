---
// src/pages/contact.astro
import Layout from "../layouts/Layout.astro";
import { getCurrentUser } from "../types/auth-server";
import { getContactInfo } from "../types/loader";

const user = await getCurrentUser(Astro.cookies);

if (!user) {
  return Astro.redirect("/login?redirect=/contact");
}

const contactInfo = await getContactInfo();
const hasCoordinates =
  contactInfo?.mapLatitude != null && contactInfo?.mapLongitude != null;
const mapSrc = hasCoordinates
  ? `https://maps.google.com/maps?q=${contactInfo!.mapLatitude},${contactInfo!.mapLongitude}&z=15&output=embed`
  : null;
---

<Layout>
  <section class="max-w-5xl mx-auto px-6 py-20">
    <div class="text-center mb-12">
      <h1 class="font-display text-4xl font-bold uppercase text-[#14171A] dark:text-white mb-3">
        Get In Touch
      </h1>
      <p class="text-gray-500 dark:text-gray-400">
        Questions about a course? Send us a message and we'll get back to
        you shortly.
      </p>
    </div>

    <div class="grid lg:grid-cols-2 gap-12 items-start">
      <div class="max-w-lg mx-auto lg:mx-0 w-full">
    <form id="contact-form" class="space-y-4">
      <!-- Honeypot field: hidden from real users via CSS, bots fill it anyway -->
      <input
        type="text"
        name="company"
        tabindex="-1"
        autocomplete="off"
        aria-hidden="true"
        class="hidden"
      />

      <div>
        <label for="name" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
          Name
        </label>
        <input
          id="name"
          name="name"
          type="text"
          required
          value={user?.username ?? ""}
          readonly={!!user}
          aria-describedby={user ? "name-hint" : undefined}
          class={`w-full px-4 py-3 rounded-md border border-[#14171A]/15 dark:border-gray-500 dark:bg-gray-700 dark:text-white focus:outline-none focus:ring-2 focus:ring-[#1F6F50] ${
            user ? "bg-gray-50 dark:bg-gray-600 text-gray-500 dark:text-gray-300 cursor-not-allowed" : ""
          }`}
        />
        {
          user && (
            <p id="name-hint" class="text-xs text-gray-400 dark:text-gray-500 mt-1">
              Filled in from your account
            </p>
          )
        }
      </div>

      <div>
        <label for="email" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
          Email
        </label>
        <input
          id="email"
          name="email"
          type="email"
          required
          value={user?.email ?? ""}
          readonly={!!user}
          aria-describedby={user ? "email-hint" : undefined}
          class={`w-full px-4 py-3 rounded-md border border-[#14171A]/15 dark:border-gray-500 dark:bg-gray-700 dark:text-white focus:outline-none focus:ring-2 focus:ring-[#1F6F50] ${
            user ? "bg-gray-50 dark:bg-gray-600 text-gray-500 dark:text-gray-300 cursor-not-allowed" : ""
          }`}
        />
        {
          user && (
            <p id="email-hint" class="text-xs text-gray-400 dark:text-gray-500 mt-1">
              Filled in from your account
            </p>
          )
        }
      </div>

      <div>
        <label for="phone" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
          Phone <span class="text-gray-400 dark:text-gray-500">(optional)</span>
        </label>
        <input
          id="phone"
          name="phone"
          type="tel"
          class="w-full px-4 py-3 rounded-md border border-[#14171A]/15 dark:border-gray-500 dark:bg-gray-700 dark:text-white focus:outline-none focus:ring-2 focus:ring-[#1F6F50]"
        />
      </div>

      <div>
        <label for="message" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
          Message
        </label>
        <textarea
          id="message"
          name="message"
          required
          rows="5"
          aria-describedby="contact-error"
          class="w-full px-4 py-3 rounded-md border border-[#14171A]/15 dark:border-gray-500 dark:bg-gray-700 dark:text-white focus:outline-none focus:ring-2 focus:ring-[#1F6F50]"
        ></textarea>
      </div>

      <p id="contact-error" class="text-sm text-red-600 hidden" role="alert" aria-live="assertive"></p>
      <p id="contact-success" class="text-sm text-[#1F6F50] dark:text-green-400 font-medium hidden" role="status" aria-live="polite">
        Thanks! Your message has been sent — we'll be in touch soon.
      </p>

      <button
        type="submit"
        id="contact-submit"
        class="w-full py-3.5 bg-[#F0A93B] hover:bg-[#dd9a30] text-[#14171A] font-bold uppercase tracking-wide text-sm rounded-md transition disabled:opacity-50 flex items-center justify-center gap-2"
      >
        <svg
          id="contact-spinner"
          class="hidden animate-spin h-4 w-4"
          viewBox="0 0 24 24"
          fill="none"
          aria-hidden="true"
        >
          <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
          <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v4a4 4 0 00-4 4H4z"></path>
        </svg>
        <span>Send Message</span>
      </button>
    </form>
      </div>

      <div class="max-w-lg mx-auto lg:mx-0 w-full">
        <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-sm p-6 mb-6">
          <h2 class="font-bold text-[#14171A] dark:text-white mb-4">Visit Us</h2>
          <ul class="space-y-3 text-sm text-gray-600 dark:text-gray-300">
            {
              contactInfo?.contactAddress && (
                <li class="flex items-start gap-3">
                  <svg class="w-5 h-5 text-[#1F6F50] dark:text-green-400 flex-shrink-0 mt-0.5" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M9.69 18.933l.003.001C9.89 19.02 10 19 10 19s.11.02.308-.066l.002-.001.006-.003.018-.008a5.741 5.741 0 00.281-.14c.186-.096.446-.24.757-.433.62-.384 1.445-.966 2.274-1.765C15.302 14.988 17 12.493 17 9A7 7 0 103 9c0 3.492 1.698 5.988 3.355 7.584a13.731 13.731 0 002.273 1.765 11.842 11.842 0 00.976.544l.062.029.018.008.006.003zM10 11.25a2.25 2.25 0 100-4.5 2.25 2.25 0 000 4.5z" clip-rule="evenodd" />
                  </svg>
                  <span>{contactInfo.contactAddress}</span>
                </li>
              )
            }
            {
              contactInfo?.contactPhone && (
                <li class="flex items-start gap-3">
                  <svg class="w-5 h-5 text-[#1F6F50] dark:text-green-400 flex-shrink-0 mt-0.5" viewBox="0 0 20 20" fill="currentColor">
                    <path d="M2 3.5A1.5 1.5 0 013.5 2h1.148a1.5 1.5 0 011.465 1.175l.716 3.223a1.5 1.5 0 01-1.052 1.767l-.933.267c-.41.117-.643.555-.48.95a11.542 11.542 0 006.254 6.254c.395.163.833-.07.95-.48l.267-.933a1.5 1.5 0 011.767-1.052l3.223.716A1.5 1.5 0 0118 15.352V16.5a1.5 1.5 0 01-1.5 1.5H15c-1.149 0-2.263-.15-3.326-.43A13.022 13.022 0 012.43 8.326 13.019 13.019 0 012 5V3.5z" />
                  </svg>
                  <a href={`tel:${contactInfo.contactPhone}`} class="hover:text-[#1F6F50] dark:hover:text-green-400 transition">
                    {contactInfo.contactPhone}
                  </a>
                </li>
              )
            }
            {
              !contactInfo?.contactAddress && !contactInfo?.contactPhone && (
                <li class="text-gray-400 dark:text-gray-500 italic">
                  Contact details coming soon.
                </li>
              )
            }
          </ul>
        </div>

        {
          mapSrc && (
            <div class="rounded-2xl overflow-hidden shadow-sm">
              <iframe
                src={mapSrc}
                width="100%"
                height="320"
                style="border:0"
                loading="lazy"
                referrerpolicy="no-referrer-when-downgrade"
                title="Our location on the map"
                class="w-full"
              />
            </div>
          )
        }
      </div>
    </div>
  </section>
</Layout>

<script>
  const form = document.getElementById("contact-form") as HTMLFormElement;
  const errorEl = document.getElementById("contact-error")!;
  const successEl = document.getElementById("contact-success")!;
  const submitBtn = document.getElementById(
    "contact-submit"
  ) as HTMLButtonElement;
  const spinner = document.getElementById("contact-spinner")!;

  form.addEventListener("submit", async (e) => {
    e.preventDefault();
    errorEl.classList.add("hidden");
    successEl.classList.add("hidden");
    submitBtn.disabled = true;
    spinner.classList.remove("hidden");

    const formData = new FormData(form);
    const payload = Object.fromEntries(formData.entries());

    try {
      const res = await fetch("/api/contact", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(payload),
      });
      const data = await res.json();

      if (!res.ok) {
        throw new Error(data?.error || "Something went wrong.");
      }

      form.reset();
      form.classList.add("hidden");
      successEl.classList.remove("hidden");
    } catch (err) {
      errorEl.textContent =
        err instanceof Error ? err.message : "Something went wrong.";
      errorEl.classList.remove("hidden");
      submitBtn.disabled = false;
      spinner.classList.add("hidden");
    }
  });
</script>